<h1>Editing Book </h1>
<%= form_for(@book1) do |f| %>
	<p>
		<%= f.label :Title %><br>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :Opinion %><br>
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.submit 'Update Book'%>
	</p>
<% end%>
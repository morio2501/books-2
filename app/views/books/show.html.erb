<table>
		<h1>Book detail</h1>
		<tbody>
			<td></td>
			<td></td>
			<td><%= @book1.title%></td>
			<td><%= @book1.body %> </td>
			<td><%=link_to 'edit', edit_book_path(@book1)%></td>
			<td><%= link_to 'Destroy',book_path(@book1), method: :delete, data: {confirm: 'Are you sure?'} %></td>
		</tr>
		</tbody>
</table>


<div class="newbook">
	<h1>New book</h1>
	<%= form_for(@book) do |f| %>
		<h4>Title</h4>
		<%= f.text_field :title %>
		<h4>Opinion</h4>
		<%= f.text_area :body %>
		<%= f.submit 'Create Book'%>
	<% end%>
</div>